<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/jpeg" href="/favicon.jpg" />
    <meta name="generator" content={Astro.generator} />
    <title>TypeScript Lesson</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  :root {
    color-scheme: dark;
    --bg-0: #1f080f;
    --bg-1: #380d19;
    --panel: rgba(92, 20, 39, 0.78);
    --panel-border: rgba(255, 154, 176, 0.42);
    --text: #f3d9e0;
    --muted: #dfb3bf;
    --accent: #ff8ea8;
    --accent-2: #ffc1d1;
  }

  * {
    box-sizing: border-box;
  }

  html,
  body {
    margin: 0;
    min-height: 100%;
  }

  body {
    min-height: 100vh;
    font-family:
      "Space Grotesk", "Noto Sans JP", "Hiragino Sans", "Yu Gothic", sans-serif;
    color: var(--text);
    background: linear-gradient(160deg, var(--bg-0), var(--bg-1));
  }

  main {
    width: min(920px, 100% - 2rem);
    margin: 2.5rem auto 3rem;
    padding: 1.5rem clamp(1rem, 2.5vw, 2rem);
    border: 1px solid var(--panel-border);
    border-radius: 4px;
    background: var(--panel);
    backdrop-filter: blur(8px);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.45);
    animation: fade-in 420ms ease-out both;
  }

  h1,
  h2 {
    letter-spacing: 0.01em;
  }

  h1 {
    margin: 0;
    font-size: clamp(2rem, 4vw, 3rem);
    line-height: 1.15;
  }

  h2 {
    margin: 1.8rem 0 0.6rem;
    font-size: clamp(1.2rem, 2.6vw, 1.5rem);
  }

  p,
  li {
    color: var(--muted);
    line-height: 1.75;
  }

  a {
    color: var(--accent);
    text-decoration-color: color-mix(in oklab, var(--accent), transparent 35%);
    text-underline-offset: 0.15em;
  }

  a:hover {
    color: var(--accent-2);
  }

  img {
    max-width: 100%;
    height: auto;
  }

  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(8px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
